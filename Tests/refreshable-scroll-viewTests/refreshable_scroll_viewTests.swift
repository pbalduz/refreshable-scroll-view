import XCTest
@testable import refreshable_scroll_view

final class refreshable_scroll_viewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(refreshable_scroll_view().text, "Hello, World!")
    }
}
